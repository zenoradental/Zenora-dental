node_modules/
.env
.DS_Store
preview-all.html
*_BACKUP/
*.zip
dist/
.vercel
.env*

# Templates are documentation, not secrets — keep them tracked.
!.env.example

# Personal Claude Code settings — machine-specific, not for the repo.
.claude/settings.local.json
